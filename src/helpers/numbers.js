/**
 * This helper function format the price
 * @param {number} price
 * @returns formated number with 3 decimals point
 * @author Khaled Nasser
 */
export const formatPrice = (price) => {
  return price.toFixed(3);
};
