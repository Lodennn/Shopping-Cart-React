/**
 * This helper function checks if the input cart is empty or not
 * @param {array} cart
 * @returns boolean
 * @author Khaled Nasser
 */
export const isCartEmpty = (cart) => {
  return cart.length === 0;
};
